rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only signed-in staff (created manually in the Firebase console —
    // there's no public sign-up) can read/write anything.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
