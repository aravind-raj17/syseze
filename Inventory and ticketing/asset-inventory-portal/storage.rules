rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Only signed-in staff can upload/download ticket attachments.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
